# Non-sensitive runtime values. Kubernetes injects these via catalog-config.
catalog:
  config:
    SERVICE_VERSION: "v1"
    READ_TIMEOUT_SECONDS: "10"
    SHUTDOWN_TIMEOUT_SECONDS: "5"
    EVENTS_URL: "http://events.prd.svc.cluster.local:8080/ingest"
    SERVICE_NAME: "catalog"

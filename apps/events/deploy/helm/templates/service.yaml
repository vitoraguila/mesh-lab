apiVersion: v1
kind: Service
metadata:
  name: events
  labels:
    {{- include "events.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: events
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      appProtocol: http

# Documentation only: Make never automatically loads this file.
# `make secrets ENV=prd` writes real values to .local/prd/grafana-secrets.json.
grafana:
  credentials:
    username: "replace-locally"
    password: "replace-locally"
  secrets: {}

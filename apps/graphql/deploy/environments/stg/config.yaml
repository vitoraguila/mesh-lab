# Non-sensitive runtime values. Kubernetes injects these via graphql-config.
graphql:
  config:
    READ_TIMEOUT_SECONDS: "10"
    SHUTDOWN_TIMEOUT_SECONDS: "5"
    EVENTS_URL: "http://events.stg.svc.cluster.local:8080/ingest"
    SERVICE_NAME: "graphql"

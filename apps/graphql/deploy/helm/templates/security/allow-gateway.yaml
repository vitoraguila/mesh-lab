apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: graphql-from-gateway
spec:
  selector:
    matchLabels:
      app: graphql
  action: ALLOW
  rules:
    - from:
        - source:
            principals:
              - "cluster.local/ns/{{ .Release.Namespace }}/sa/{{ .Values.global.gateway.serviceAccount }}"
      to:
        - operation:
            methods: [POST]
            ports: ["8080"]

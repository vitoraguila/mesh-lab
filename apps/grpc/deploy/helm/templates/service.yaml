apiVersion: v1
kind: Service
metadata:
  name: grpc
  labels:
    {{- include "grpc.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: grpc
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: http
      appProtocol: grpc

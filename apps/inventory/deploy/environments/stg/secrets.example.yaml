# Documentation only: Make never automatically loads this file.
# Copy to .local/stg/inventory-secrets.json (JSON/YAML accepted by Helm).
# Only define secrets that the application actually consumes.
inventory:
  secrets: {}
  # Example for a future database integration:
  # secrets:
  #   DATABASE_PASSWORD: "replace-locally"
  # Alternatively set existingEnvSecret: inventory-external-env

apiVersion: v1
kind: Service
metadata:
  name: inventory
  labels:
    {{- include "inventory.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: inventory
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      appProtocol: http

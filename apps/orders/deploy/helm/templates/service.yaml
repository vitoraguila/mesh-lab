apiVersion: v1
kind: Service
metadata:
  name: orders
  labels:
    {{- include "orders.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: orders
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      appProtocol: http

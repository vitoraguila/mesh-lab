apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: payments
spec:
  exportTo: ["."]
  host: payments.{{ .Release.Namespace }}.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    {{- toYaml .Values.trafficPolicy | nindent 4 }}

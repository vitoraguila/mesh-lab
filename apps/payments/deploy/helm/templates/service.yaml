apiVersion: v1
kind: Service
metadata:
  name: payments
  labels:
    {{- include "payments.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: payments
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      appProtocol: http

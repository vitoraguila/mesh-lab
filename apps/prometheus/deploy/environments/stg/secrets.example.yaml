# Documentation only: Make never automatically loads this file.
# Copy to .local/stg/prometheus-secrets.yaml when a scrape target needs a credential.
prometheus:
  secrets: {}
  # Alternatively set existingEnvSecret: prometheus-external-env

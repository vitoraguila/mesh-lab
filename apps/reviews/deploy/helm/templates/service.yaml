apiVersion: v1
kind: Service
metadata:
  name: reviews
  labels:
    {{- include "reviews.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: reviews
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      appProtocol: http
